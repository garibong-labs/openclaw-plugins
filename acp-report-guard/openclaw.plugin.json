{
  "id": "acp-report-guard",
  "name": "ACP Report Guard",
  "description": "Validates canonical ACP lifecycle reports, cancels malformed ones before delivery, and restricts agent-started ACP launch routes to the main agent.",
  "version": "0.2.0",
  "activation": {
    "onStartup": true,
    "onCapabilities": ["hook"]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enforce": {
        "type": "boolean",
        "default": true,
        "description": "Cancel malformed lifecycle reports and block direct intermediate publication. Set false to classify and log only."
      },
      "blockDirectIntermediateToolCalls": {
        "type": "boolean",
        "default": true,
        "description": "Block direct message-tool publication of ACP intermediate reports."
      },
      "blockNonMainAcpLaunches": {
        "type": "boolean",
        "default": true,
        "description": "Block recognized ACP launch routes when the hook context agent id is not exactly 'main'."
      },
      "maxIntermediateChars": {
        "type": "integer",
        "minimum": 200,
        "maximum": 4000,
        "default": 1400,
        "description": "Character ceiling for the ten-minute cadence report."
      },
      "maxBoundaryReportChars": {
        "type": "integer",
        "minimum": 200,
        "maximum": 4000,
        "default": 2000,
        "description": "Character ceiling for start, correction-start, and completion reports."
      }
    }
  },
  "uiHints": {
    "enforce": {
      "label": "Enforce",
      "help": "Turn off for a live message_sending cancellation smoke before activation."
    },
    "blockDirectIntermediateToolCalls": {
      "label": "Block direct intermediate sends",
      "advanced": true
    },
    "blockNonMainAcpLaunches": {
      "label": "Block non-main ACP launches",
      "advanced": true
    },
    "maxIntermediateChars": {
      "label": "Intermediate character ceiling",
      "advanced": true
    },
    "maxBoundaryReportChars": {
      "label": "Boundary report character ceiling",
      "advanced": true
    }
  }
}
